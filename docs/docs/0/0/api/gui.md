# Module ura.gui

## Functions

`connect(sid, environ)`
:

`connect_error(data)`
:

`disconnect(sid)`
:

`texc(func: Callable[[Any], Any]) ‑> Callable[[Any], Any]`
:

## Classes

`Expose()`
:

```
### Methods
```

`cfg_path(*args, **kwargs)`
:

`config(*args, **kwargs)`
:

`dl(*args, **kwargs)`
:

`write_config(*args, **kwargs)`
:
